import 'package:flutter/material.dart';

class MarkQuestions extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(
        title: const Text("Đã lưu"),
      ),
      body: Center(child: Text("Đã lưu"),),
    );
  }
}