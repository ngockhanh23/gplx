class Test {
  int id;
  String title;
  String status;
  String description;
  int correctQuestionNumber;
  int wrongQuestionNumber;

  Test(this.id, this.title, this.status, this.description,
      this.correctQuestionNumber, this.wrongQuestionNumber);
}